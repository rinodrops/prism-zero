# Docs assets

Optional copies of Marketplace README images. Prefer linking screenshots directly:

`https://raw.githubusercontent.com/rinodrops/prism-zero/main/screenshots/light-nova.webp`

Keep WebP here in sync with `screenshots/*.webp` when present. Do not put these inside the `.novaextension` bundle — the Library does not render bundled README images.
